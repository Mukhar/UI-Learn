// Hooks Deep Dive — beyond the basics: layout effects, lazy init,
// useId, useTransition, useSyncExternalStore, custom hook recipes.
window.MODULES = window.MODULES || [];
window.MODULES.push({
  id: "hooks-deep",
  title: "Hooks Deep Dive",
  blurb: "Beyond the basics: niche hooks, custom hook recipes, gotchas.",
  questions: [
    {
      id: "hd-1",
      difficulty: "medium",
      q: "`useEffect` vs `useLayoutEffect` — when MUST you reach for the latter?",
      hint: "Synchronous DOM measurement before paint.",
      answer: `Both have the same signature and dependency semantics. The difference is WHEN they run:\n- \`useEffect\` fires **after** the browser paints the new DOM (async, non-blocking).\n- \`useLayoutEffect\` fires **synchronously after DOM mutations but before paint** — it blocks the browser from painting until done.\n\nUse \`useLayoutEffect\` when you must read layout (\`getBoundingClientRect\`, scroll position) and then mutate the DOM/state BEFORE the user sees a flicker. Classic case: positioning a tooltip relative to its anchor, or measuring text width to set a font-size. Otherwise default to \`useEffect\` — \`useLayoutEffect\` blocks paint and warns during SSR (since there's no DOM to measure on the server).`,
      keyPoints: [
        "useEffect = async, after paint",
        "useLayoutEffect = sync, before paint",
        "Use case: measure + mutate to avoid flicker",
        "SSR warning for useLayoutEffect",
      ],
    },
    {
      id: "hd-2",
      difficulty: "medium",
      q: "What is the `useState` lazy initializer and when is it worth it?",
      hint: "Pass a function instead of a value.",
      answer: `\`useState(initial)\` evaluates \`initial\` on EVERY render — even though React only uses it on the first one. If computing the initial value is expensive (parsing localStorage, computing from a big array), wrap it in a function:\n\`\`\`js\nconst [state, setState] = useState(() => JSON.parse(localStorage.getItem('cart')) ?? []);\n\`\`\`\nReact only calls the function once, on mount. Same applies to \`useReducer(reducer, initialArg, init)\` — \`init\` is the lazy initializer there.`,
      keyPoints: [
        "Value form runs every render, lazy runs once",
        "Example with localStorage / heavy compute",
        "Same pattern in useReducer's init arg",
      ],
    },
    {
      id: "hd-3",
      difficulty: "medium",
      q: "Explain `useContext` re-render behavior. How do you avoid re-rendering every consumer when one slice of context changes?",
      hint: "Context has no selectors — split or use external store.",
      answer: `Every component calling \`useContext(MyContext)\` re-renders whenever the context VALUE reference changes — there's no selector mechanism. So a single object context with \`{ user, theme, cart }\` re-renders all consumers when ANY field changes.\n\nFixes:\n1. **Split contexts** by update frequency: \`UserContext\`, \`ThemeContext\`, \`CartContext\` — each consumer subscribes only to what it needs.\n2. **Memoize the value**: \`<Ctx.Provider value={useMemo(() => ({ user, setUser }), [user])} />\` so the value reference is stable.\n3. **External store + useSyncExternalStore** (or libraries like Zustand/Jotai) for selector-based subscriptions.\n4. Pass children as a prop and let \`React.memo\` skip subtrees that don't care.`,
      keyPoints: [
        "All consumers re-render on value change — no selectors",
        "Fix 1: split contexts",
        "Fix 2: memoize provider value",
        "Fix 3: external store / useSyncExternalStore",
      ],
    },
    {
      id: "hd-4",
      difficulty: "medium",
      q: "What is `useId` for? Why not just use `Math.random()` or a module-level counter?",
      hint: "SSR hydration + stable across renders.",
      answer: `\`useId\` returns a unique string ID that is **stable across re-renders** and **identical on server and client** during SSR — so it works for hydration without mismatches. It's designed for accessibility attributes like \`htmlFor\`, \`aria-describedby\`, \`aria-labelledby\` where you need to link an input to a label without conflicting IDs across instances.\n\`\`\`jsx\nfunction Field({ label }) {\n  const id = useId();\n  return (<><label htmlFor={id}>{label}</label><input id={id} /></>);\n}\n\`\`\`\nWhy not \`Math.random()\`? Different value every render + server/client mismatch -> hydration error. Module counters break under SSR because the server's counter state leaks across requests.`,
      keyPoints: [
        "Stable across renders",
        "Same on server + client (hydration safe)",
        "Designed for a11y attribute linking",
        "Why random/counter fails (mismatch / SSR leak)",
      ],
    },
    {
      id: "hd-5",
      difficulty: "hard",
      q: "Write a custom hook `useLocalStorage(key, initial)` that syncs state to localStorage AND across tabs.",
      hint: "useState + useEffect + `storage` event.",
      answer: `\`\`\`js\nfunction useLocalStorage(key, initial) {\n  const [value, setValue] = useState(() => {\n    try {\n      const raw = localStorage.getItem(key);\n      return raw !== null ? JSON.parse(raw) : initial;\n    } catch {\n      return initial;\n    }\n  });\n\n  // Persist on change\n  useEffect(() => {\n    try { localStorage.setItem(key, JSON.stringify(value)); } catch {}\n  }, [key, value]);\n\n  // Sync across tabs\n  useEffect(() => {\n    const onStorage = (e) => {\n      if (e.key === key && e.newValue !== null) {\n        try { setValue(JSON.parse(e.newValue)); } catch {}\n      }\n    };\n    window.addEventListener('storage', onStorage);\n    return () => window.removeEventListener('storage', onStorage);\n  }, [key]);\n\n  return [value, setValue];\n}\n\`\`\`\nNotes: lazy initializer avoids re-reading localStorage every render; try/catch guards against quota errors and JSON parse failures; the \`storage\` event fires only in OTHER tabs (not the one that wrote), giving cross-tab sync for free.`,
      keyPoints: [
        "Lazy initializer reads from localStorage once",
        "Effect persists on change with key in deps",
        "Listens to 'storage' event for cross-tab sync",
        "try/catch around JSON + quota errors",
      ],
    },
    {
      id: "hd-6",
      difficulty: "hard",
      q: "Write a custom hook `useFetch(url)` that aborts the request on unmount or URL change.",
      hint: "AbortController in cleanup.",
      answer: `\`\`\`js\nfunction useFetch(url) {\n  const [state, setState] = useState({ data: null, error: null, loading: true });\n\n  useEffect(() => {\n    const ctrl = new AbortController();\n    setState({ data: null, error: null, loading: true });\n\n    fetch(url, { signal: ctrl.signal })\n      .then(r => {\n        if (!r.ok) throw new Error(\`HTTP \${r.status}\`);\n        return r.json();\n      })\n      .then(data => setState({ data, error: null, loading: false }))\n      .catch(err => {\n        if (err.name === 'AbortError') return; // unmount / url change, ignore\n        setState({ data: null, error: err, loading: false });\n      });\n\n    return () => ctrl.abort();\n  }, [url]);\n\n  return state;\n}\n\`\`\`\nWhy it matters: without the abort, a stale response from a previous URL can overwrite the current one (race condition), AND you'd set state on an unmounted component (memory leak warning in older React). For real apps, use TanStack Query instead — caching, dedup, retries, focus refetch, etc.`,
      keyPoints: [
        "AbortController + signal",
        "Cleanup aborts pending request",
        "Ignores AbortError in catch",
        "Mentions race condition + recommends Query lib",
      ],
    },
    {
      id: "hd-7",
      difficulty: "hard",
      q: "What is `useTransition` vs `useDeferredValue`? When pick which?",
      hint: "Mark an UPDATE non-urgent vs mark a VALUE non-urgent.",
      answer: `Both opt into React 18's concurrent rendering for non-urgent work, but from different angles:\n\n**\`useTransition\`** — you control the UPDATE. You wrap state setters that should be low-priority:\n\`\`\`js\nconst [isPending, startTransition] = useTransition();\nstartTransition(() => setFilter(newFilter));\n\`\`\`\nGood when you OWN the state setter.\n\n**\`useDeferredValue\`** — you control the VALUE. You take an external value (e.g., a prop you don't control) and get a "lagging" copy that React updates as time allows:\n\`\`\`js\nconst deferredQuery = useDeferredValue(query);\n// pass deferredQuery to expensive child\n\`\`\`\nGood when the source state lives elsewhere (a prop, a controlled input).\n\nBoth let urgent updates (the typed character) win over slow ones (the giant filtered list). Pair with \`React.memo\` so the heavy child only re-renders when the deferred value actually changes.`,
      keyPoints: [
        "Transition wraps the SETTER, you own the state",
        "DeferredValue wraps a VALUE you don't own",
        "Both yield to urgent updates",
        "React.memo on heavy child to actually skip work",
      ],
    },
    {
      id: "hd-8",
      difficulty: "hard",
      q: "What is `useSyncExternalStore` and why does it exist?",
      hint: "Subscribing to non-React stores in a concurrent-safe way.",
      answer: `Introduced in React 18 to let libraries subscribe to **external mutable stores** (Redux, Zustand, browser APIs like \`window.matchMedia\`) safely under concurrent rendering. Signature:\n\`\`\`js\nconst value = useSyncExternalStore(subscribe, getSnapshot, getServerSnapshot?);\n\`\`\`\n- \`subscribe(callback)\` registers \`callback\` to fire on store change, returns an unsubscribe.\n- \`getSnapshot()\` returns the current value; React calls it during render and uses \`Object.is\` to detect change.\n- \`getServerSnapshot()\` provides the SSR value to avoid hydration mismatch.\n\nWhy it exists: concurrent rendering can render the same component multiple times with different priorities, so reading a mutable external value via \`useEffect\` + state could "tear" (different parts of the tree seeing different values). \`useSyncExternalStore\` guarantees a consistent snapshot across the render. Example use: a \`useMediaQuery\` hook:\n\`\`\`js\nconst isDark = useSyncExternalStore(\n  (cb) => { const m = matchMedia('(prefers-color-scheme: dark)'); m.addEventListener('change', cb); return () => m.removeEventListener('change', cb); },\n  () => matchMedia('(prefers-color-scheme: dark)').matches,\n  () => false\n);\n\`\`\``,
      keyPoints: [
        "Built for external mutable stores",
        "Three args: subscribe, getSnapshot, getServerSnapshot",
        "Prevents tearing under concurrent rendering",
        "Real example (Redux/Zustand/matchMedia)",
      ],
    },
    {
      id: "hd-9",
      difficulty: "hard",
      q: "Effect cleanup runs in what ORDER for nested components, and why does it matter?",
      hint: "Children unmount before parents.",
      answer: `On unmount, React runs cleanups **child-first, then parent** — the inverse of mount order. On dependency change, it also runs the cleanup of the affected effect BEFORE the new effect body. This ordering matters because:\n\n1. **Subscriptions**: a child subscribed to a parent-provided resource can unsubscribe before the parent tears the resource down — no "subscribing to a disposed object" bug.\n2. **Refs**: a parent's \`useEffect\` runs AFTER all child effects mount, so it can rely on child refs being attached. Conversely on unmount, parent cleanup runs AFTER children, so it can still see the tree it set up.\n3. **Strict Mode in dev**: React intentionally mounts -> unmounts -> remounts components to flush out missing cleanups. If you forget to clean up a subscription, you'll see double listeners immediately — that's the point.`,
      keyPoints: [
        "Unmount/cleanup order: children before parents",
        "Mount order: parents' effects run after children's",
        "Why it matters: safe teardown of shared resources",
        "Strict Mode double-invoke catches missing cleanup",
      ],
    },
    {
      id: "hd-10",
      difficulty: "hard",
      q: "Common cause and fix for the `react-hooks/exhaustive-deps` warning when an object/function is the dependency?",
      hint: "Referential inequality every render.",
      answer: `The lint rule complains because each render creates a new reference for inline objects/functions/arrays, which would cause the effect to re-run on EVERY render. Cheap fixes:\n\n1. **Move the dependency inside the effect** if it's only used there — no need to list it.\n2. **Memoize** the offending value: \`const config = useMemo(() => ({ id }), [id]);\` / \`useCallback\`.\n3. **Destructure primitives** out and depend on those: \`const { id } = obj; useEffect(..., [id])\` instead of \`[obj]\`.\n4. **Functional setState** so you don't need to depend on state: \`setCount(c => c + 1)\` instead of \`setCount(count + 1)\`.\n5. **Refs** for "I want to read the latest value but not re-run on change": store in \`ref.current\` and update via a separate effect.\n\nNuclear option: \`// eslint-disable-next-line react-hooks/exhaustive-deps\` — only when you understand exactly why, with a comment explaining the invariant. Disabling the rule blindly is how stale-closure bugs ship to prod.`,
      keyPoints: [
        "New reference every render = always re-runs",
        "Fix: move inside, memoize, or destructure primitive",
        "Functional setState avoids depending on state",
        "Ref for read-latest without re-run",
        "Disable comment only with explanation",
      ],
    },
  ],
});
