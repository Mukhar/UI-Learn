// Modern React (18/19) — interview-focused: concurrent rendering,
// Suspense for data, RSC, Actions, useOptimistic, use(), React Compiler.
window.MODULES = window.MODULES || [];
window.MODULES.push({
  id: "modern-react",
  title: "Modern React (18 & 19)",
  blurb: "Concurrent rendering, RSC, Actions, use(), useOptimistic, React Compiler — the stuff modern interviewers actually ask.",
  questions: [
    {
      id: "mr-1",
      difficulty: "easy",
      q: "What are the headline features of **React 18**, and which one is the foundation for everything else?",
      hint: "Concurrent renderer is the plumbing that unlocks the rest.",
      answer: `React 18 shipped a **new concurrent renderer** — that's the foundation. On top of it:\n\n1. **Automatic batching** — state updates across promises, timeouts, and native handlers are batched (in 17 only inside React event handlers).\n2. **Transitions** — \`startTransition\` / \`useTransition\` to mark updates as non-urgent so React can interrupt them.\n3. **Suspense for data fetching** — Suspense boundaries can now suspend on async data (with a framework like Next.js/Relay or the new \`use()\` hook in 19).\n4. **New Suspense SSR** — streaming HTML + selective hydration (below).\n5. **New hooks** — \`useId\`, \`useTransition\`, \`useDeferredValue\`, \`useSyncExternalStore\`, \`useInsertionEffect\` (CSS-in-JS libs only).\n6. **Strict Mode double-invoke** in dev — surface missing cleanups.\n7. **New root API** — \`createRoot\` replaces \`ReactDOM.render\`; opting in is what turns concurrent features on.\n\nInterview soundbite: "React 18 turned rendering from synchronous-and-blocking into interruptible-and-prioritizable. Everything else is a consequence of that."`,
      keyPoints: [
        "createRoot / concurrent renderer is the base",
        "Automatic batching everywhere",
        "Transitions API",
        "Suspense for data + streaming SSR",
        "New hooks: useId, useTransition, useDeferredValue, useSyncExternalStore",
      ],
    },
    {
      id: "mr-2",
      difficulty: "medium",
      q: "Explain **automatic batching** in React 18. What changed vs React 17, and how do you opt OUT when you need synchronous updates?",
      hint: "Promises, timeouts, native events are now batched. `flushSync` opts out.",
      answer: `In React 17, multiple \`setState\` calls were batched only when they happened inside a React event handler (like \`onClick\`). Anywhere else — inside a \`setTimeout\`, a \`fetch().then\`, a \`Promise\` microtask, a native \`addEventListener\` callback — each \`setState\` triggered its own render.\n\nReact 18 batches them ALL by default once you're on \`createRoot\`:\n\`\`\`js\nfetch('/api').then(() => {\n  setCount(c => c + 1);   // 17: render\n  setFlag(true);          // 17: render     -> 18: single render\n});\n\`\`\`\n\nOpt-out with \`flushSync\` when you MUST see the DOM updated between two setters (rare — usually for measuring or integrating with imperative libs):\n\`\`\`js\nimport { flushSync } from 'react-dom';\nflushSync(() => setTab('two'));\ntabRef.current?.scrollIntoView(); // DOM already reflects tab 'two'\n\`\`\``,
      keyPoints: [
        "17: batching only in React event handlers",
        "18: batched in promises/timeouts/native handlers too",
        "Fewer renders = better perf, mostly transparent",
        "flushSync(fn) to force a synchronous flush",
      ],
    },
    {
      id: "mr-3",
      difficulty: "medium",
      q: "What is `startTransition` and when should you reach for it? Give a realistic example.",
      hint: "Mark a state update as non-urgent so typing/input stays responsive.",
      answer: `\`startTransition\` (and its hook form \`useTransition\`) marks a state update as **non-urgent**. React can interrupt the resulting render if a higher-priority update (like a keystroke) arrives — no more janky typing while a giant list re-filters.\n\n\`\`\`js\nfunction Search() {\n  const [query, setQuery] = useState('');\n  const [results, setResults] = useState([]);\n  const [isPending, startTransition] = useTransition();\n\n  const onChange = (e) => {\n    setQuery(e.target.value);                   // urgent — updates input NOW\n    startTransition(() => {\n      setResults(filterHugeList(e.target.value)); // non-urgent — can be interrupted\n    });\n  };\n\n  return (<>\n    <input value={query} onChange={onChange} />\n    {isPending && <Spinner />}\n    <List items={results} />\n  </>);\n}\n\`\`\`\n\nRule of thumb: use it when a user-driven update triggers an expensive downstream render. The input stays snappy; the heavy work yields. \`isPending\` gives you a "we're catching up" flag for UI feedback.`,
      keyPoints: [
        "Marks setState as low priority",
        "Urgent updates (typing) preempt the transition",
        "isPending flag for loading UX",
        "Classic example: search input + expensive filtered list",
      ],
    },
    {
      id: "mr-4",
      difficulty: "hard",
      q: "What is the new `use()` hook in React 19? How is it different from every other hook?",
      hint: "It reads promises AND context — and it CAN sit inside conditionals/loops.",
      answer: `\`use()\` is a first-class primitive that lets a component **read the value of a resource** — a Promise or a Context — during render.\n\n\`\`\`jsx\nimport { use } from 'react';\n\nfunction Comments({ commentsPromise }) {\n  const comments = use(commentsPromise); // suspends until resolved\n  return <ul>{comments.map(c => <li key={c.id}>{c.text}</li>)}</ul>;\n}\n\n// Parent wraps in Suspense so React knows what fallback to show:\n<Suspense fallback={<Spinner />}>\n  <Comments commentsPromise={fetchComments()} />\n</Suspense>\n\`\`\`\n\nWhat makes \`use()\` special:\n1. **Not restricted by the Rules of Hooks** — unlike \`useState\`/\`useEffect\`, it CAN be called inside \`if\` blocks, loops, or \`try/catch\`. This is huge.\n2. **Suspends** when given an unresolved promise — the closest Suspense boundary shows its fallback.\n3. Also reads context: \`const theme = use(ThemeContext)\` — behaves like \`useContext\` but with the conditional-friendly rules.\n4. Designed to work with Server Components: the server can create a Promise and pass it down; the client reads it with \`use()\`.\n\nInterview trap: don't call \`fetch()\` inline in render — you'll create a new promise every render and loop forever. Cache the promise (via RSC, a framework's cache, or React's \`cache()\` helper) or pass it in as a prop.`,
      keyPoints: [
        "Reads a Promise or Context inside render",
        "Suspends on unresolved Promise -> nearest <Suspense> fallback",
        "Legal inside conditionals/loops — unlike other hooks",
        "Must NOT create a fresh Promise every render (infinite loop)",
        "Pairs with Server Components: server creates promise, client uses it",
      ],
    },
    {
      id: "mr-5",
      difficulty: "hard",
      q: "**Server Components (RSC)** vs traditional **SSR** — what's actually different, and what can/can't you do in an RSC?",
      hint: "SSR renders once on the server then hydrates. RSCs never ship to the client at all.",
      answer: `**Traditional SSR**: the server runs your React tree once and sends HTML, then the SAME components ship as JS to the client and hydrate — so the client re-runs them to attach handlers. Every component's code lives in the client bundle.\n\n**Server Components** are a different beast: they run ONLY on the server, and their **JavaScript never ships to the client**. React serializes the rendered output (not HTML — a special JSON-ish tree) and streams it to the client, which slots it into the existing tree without hydrating that piece.\n\nWhat you gain:\n- **Zero JS cost** for server-only components. A big Markdown renderer? Doesn't add a byte to the bundle.\n- **Direct backend access** — \`await db.query(...)\` inside a component, no API layer needed.\n- **Secrets stay on the server** — env vars, DB credentials, etc.\n\nWhat you LOSE inside an RSC:\n-  No \`useState\`, \`useEffect\`, or any hook that requires client state.\n-  No event handlers (\`onClick\`, \`onChange\`).\n-  No browser APIs (\`window\`, \`localStorage\`).\n-  No Context (mostly — you use props or the RSC-aware equivalents).\n\nRSCs can IMPORT and render Client Components (marked \`'use client'\`), but not the reverse in the tree structure — a Client Component can only receive an RSC as \`children\` (a rendered tree), not import one.\n\nMental model: RSCs are for data & structure; Client Components are for interactivity.`,
      keyPoints: [
        "SSR: renders on server AND ships JS to hydrate on client",
        "RSC: server-only, code never reaches the browser",
        "RSC benefits: zero JS cost, direct DB/backend access, secrets safe",
        "RSC limits: no state, effects, event handlers, or browser APIs",
        "RSC can render Client Components; Client can only receive RSC as children",
      ],
    },
    {
      id: "mr-6",
      difficulty: "medium",
      q: "Explain the `'use client'` and `'use server'` directives. Where do they go and what do they mean?",
      hint: "They mark boundaries — not individual functions inside a file.",
      answer: `Both are **top-of-file directives** (must be the first line, before imports) that tell the bundler/React which environment a module belongs to.\n\n**\`'use client'\`** — this file (and its exports) is a **Client Component boundary**. Everything imported from here runs on the client with full hooks/events/browser APIs. In an RSC-capable framework (Next.js App Router), files default to server; \`'use client'\` opts a file into the client bundle.\n\`\`\`jsx\n'use client';\nimport { useState } from 'react';\nexport function Counter() { const [n, setN] = useState(0); /* ... */ }\n\`\`\`\n\n**\`'use server'\`** — marks the exports of this file as **Server Actions**: functions safe to call from the client but that execute on the server. You can also put \`'use server'\` at the top of an individual function inside a Server Component.\n\`\`\`js\n'use server';\nexport async function createTodo(formData) {\n  await db.todos.insert({ text: formData.get('text') });\n}\n// From a client component:\n// <form action={createTodo}> ... </form>\n\`\`\`\n\nGotchas:\n- \`'use client'\` doesn't mean "runs ONLY on client" — Client Components still pre-render on the server for SSR. It means "this code is part of the client bundle and can use client-only APIs."\n- \`'use server'\` is NOT the opposite of \`'use client'\`. RSCs are server by default without any directive. \`'use server'\` specifically exposes a function as an RPC endpoint.`,
      keyPoints: [
        "'use client' = client-bundle boundary (hooks/events allowed)",
        "'use server' = mark function(s) as Server Actions (RPC-style)",
        "Both go at the very top of the file",
        "'use client' still SSRs — it doesn't mean client-only execution",
        "Default in App Router is server (no directive)",
      ],
    },
    {
      id: "mr-7",
      difficulty: "hard",
      q: "What are **Actions** in React 19? Sketch a form using an Action + `useActionState` + `useFormStatus`.",
      hint: "Pass an async function to <form action={fn}>. Get pending + result via hooks.",
      answer: `Actions unify form submission, pending states, and optimistic updates. Pass an async function directly to a form's \`action\` prop — React handles the submit, wires up pending state, and (with Server Actions) can even work without JS via progressive enhancement.\n\n\`\`\`jsx\n'use client';\nimport { useActionState } from 'react';\nimport { useFormStatus } from 'react-dom';\n\nasync function updateName(prevState, formData) {\n  const name = formData.get('name');\n  const err = await saveToDb(name);\n  if (err) return { error: err.message };\n  return { name };\n}\n\nfunction SubmitButton() {\n  const { pending } = useFormStatus();  // reads from nearest parent <form>\n  return <button disabled={pending}>{pending ? 'Saving…' : 'Save'}</button>;\n}\n\nexport function NameForm() {\n  const [state, formAction, isPending] = useActionState(updateName, { name: '' });\n  return (\n    <form action={formAction}>\n      <input name=\"name\" defaultValue={state.name} />\n      <SubmitButton />\n      {state.error && <p className=\"text-red-600\">{state.error}</p>}\n    </form>\n  );\n}\n\`\`\`\n\nWhy it's a big deal:\n- **No manual \`useState\` for pending/error/result** — \`useActionState\` returns \`[state, boundAction, isPending]\`.\n- **\`useFormStatus\`** lets any descendant of a \`<form>\` read its pending state without prop drilling — perfect for a shared \`<SubmitButton>\` component.\n- Works with Server Actions (\`'use server'\` functions) for zero-JS forms, but also fine with regular client-side async functions.\n- Automatic error handling via the returned state shape.\n\nInterview trap: \`useFormStatus\` MUST be called from a component **rendered inside** the \`<form>\` — not the component that owns the form.`,
      keyPoints: [
        "<form action={fn}> takes an async function directly",
        "useActionState -> [state, boundAction, isPending]",
        "useFormStatus -> pending state for descendants of <form>",
        "Works with Server Actions for progressive enhancement",
        "useFormStatus only works INSIDE the form subtree, not on the form owner",
      ],
    },
    {
      id: "mr-8",
      difficulty: "hard",
      q: "Show me a **like button** using `useOptimistic` — the whole point being instant UI feedback with rollback on failure.",
      hint: "useOptimistic gives you a temporary state that overrides real state until the async action settles.",
      answer: `\`useOptimistic\` lets you show a predicted state immediately, while an async action runs in the background. If the action succeeds, the real state catches up. If it throws (or you don't set the real state), React automatically reverts to the last committed real state.\n\n\`\`\`jsx\n'use client';\nimport { useOptimistic, useState, useTransition } from 'react';\n\nexport function LikeButton({ postId, initialLikes, initiallyLiked }) {\n  const [state, setState] = useState({ likes: initialLikes, liked: initiallyLiked });\n  const [optimistic, addOptimistic] = useOptimistic(\n    state,\n    (current, action) => action === 'toggle'\n      ? { likes: current.likes + (current.liked ? -1 : 1), liked: !current.liked }\n      : current\n  );\n  const [, startTransition] = useTransition();\n\n  const onClick = () => {\n    startTransition(async () => {\n      addOptimistic('toggle');           // UI updates NOW\n      try {\n        const result = await api.toggleLike(postId);  // real request\n        setState(result);                              // commit real state\n      } catch {\n        // no setState -> optimistic value discarded, UI snaps back\n      }\n    });\n  };\n\n  return (\n    <button onClick={onClick} aria-pressed={optimistic.liked}>\n      {optimistic.liked ? 'Liked' : 'Like'} — {optimistic.likes}\n    </button>\n  );\n}\n\`\`\`\n\nKey rules:\n- \`useOptimistic\` MUST be called inside a transition (or an Action). Outside one, it throws.\n- The optimistic value only lasts until the transition/action completes. Then React uses the real state again.\n- No manual rollback needed — omit the successful \`setState\` and React reverts for you.\n- Perfect fit for: likes, follows, comment posting, todo checkbox toggles, delete-with-undo, etc.`,
      keyPoints: [
        "useOptimistic(current, reducer) -> [optimisticValue, addOptimistic]",
        "Must be called from within a Transition or Action",
        "Automatic rollback if you don't commit real state",
        "Reducer computes the predicted next state",
        "Great for likes/follows/checkboxes/etc.",
      ],
    },
    {
      id: "mr-9",
      difficulty: "hard",
      q: "What problems do **streaming SSR** and **selective hydration** solve? Explain in terms of the pre-18 SSR pain points.",
      hint: "Pre-18 SSR was all-or-nothing at every step.",
      answer: `Pre-React 18, SSR had three all-or-nothing steps that blocked each other:\n\n1. **Fetch everything** on the server before rendering could start.\n2. **Render the whole tree** to HTML before sending any bytes.\n3. **Hydrate the whole tree** before ANY of it became interactive.\n\nSo a slow database call at the bottom of the page delayed the entire HTML response. And a giant hydration bundle delayed interactivity for the WHOLE page, even the parts already visible.\n\nReact 18 fixed both:\n\n**Streaming SSR** (via \`renderToPipeableStream\` / \`renderToReadableStream\`):\n- Wrap slow parts in \`<Suspense fallback={<Skeleton />}>\`.\n- The server sends HTML for the fast parts immediately, with the skeleton in place of the slow part.\n- When the slow data resolves, the server streams additional HTML chunks that React uses to swap the skeleton in place (via inline scripts).\n- **Result**: fast Time-To-First-Byte and fast First Contentful Paint, even if one section is slow.\n\n**Selective Hydration**:\n- Once HTML arrives, React hydrates components as their JS chunks arrive (via code splitting).\n- If a user interacts with a not-yet-hydrated part (click on a sidebar link before the huge dashboard has hydrated), React **prioritizes** hydration for that subtree.\n- **Result**: no more "page looks ready but nothing clicks for 3 seconds."\n\nInterview one-liner: "React 18 turned SSR from three synchronous barriers into a streaming pipeline where slow parts don't block fast ones, and interactive parts hydrate in priority order."`,
      keyPoints: [
        "Old SSR: fetch-all -> render-all -> hydrate-all, each blocking",
        "Suspense boundaries mark chunks that can be streamed independently",
        "renderToPipeableStream / renderToReadableStream",
        "Selective hydration = interaction bumps priority",
        "Fixes both TTFB/FCP AND time-to-interactive per subtree",
      ],
    },
    {
      id: "mr-10",
      difficulty: "hard",
      q: "What does the **React Compiler** (React 19) do, and should you still write `useMemo`/`useCallback`/`React.memo` after enabling it?",
      hint: "It auto-memoizes at build time. Manual memo becomes mostly redundant.",
      answer: `The React Compiler is a build-time optimizing compiler (based on the Forget project) that analyzes your components and **automatically memoizes values, JSX, and children** where safe. It understands React's Rules and emits code equivalent to hand-written \`useMemo\`/\`useCallback\` around the right pieces — without you writing them.\n\nBefore:\n\`\`\`jsx\nfunction List({ items, filter }) {\n  const visible = items.filter(i => i.type === filter);   // re-runs every render\n  const onClick = (id) => console.log(id);                // new fn every render\n  return <Rows items={visible} onClick={onClick} />;\n}\n\`\`\`\n\nAfter compiler (conceptually):\n\`\`\`jsx\nfunction List({ items, filter }) {\n  const visible = useMemo(() => items.filter(i => i.type === filter), [items, filter]);\n  const onClick = useCallback((id) => console.log(id), []);\n  return <Rows items={visible} onClick={onClick} />;\n}\n\`\`\`\n\nShould you still write memo hooks manually?\n- **Mostly no** — the compiler covers the 90% case and does it more correctly (no missed deps, no over-memoizing).\n- **Sometimes yes**: escape hatches when the compiler bails (e.g., mutations, breaking Rules of React, external library quirks) — the compiler skips those files and you'd fall back to manual memo.\n- \`React.memo\` on component boundaries can still make sense for expensive components with stable props, though the compiler often makes it unnecessary.\n\nRequirements: your components must follow the Rules of React (no mutations of props/state, no side effects during render). The compiler ships an ESLint plugin (\`eslint-plugin-react-compiler\`) that flags violations so it can safely optimize.\n\nInterview soundbite: "The compiler makes memoization a build-time concern instead of a developer-discipline concern — provided your components are pure."`,
      keyPoints: [
        "Build-time auto-memoization of values, callbacks, JSX",
        "Requires components to obey Rules of React (no mutation, pure render)",
        "Mostly removes need for useMemo/useCallback/React.memo",
        "ESLint plugin flags code the compiler will bail on",
        "Escape hatches exist; not magic for impure code",
      ],
    },
    {
      id: "mr-11",
      difficulty: "medium",
      q: "What causes **hydration mismatch** errors in modern React, and how do you fix each common flavor?",
      hint: "Server HTML must equal client's first render.",
      answer: `Hydration mismatches happen when the HTML the server produced doesn't match what the client renders on the first pass. React logs a warning (or, in stricter modes, blows up and re-renders client-side, losing streaming benefits).\n\nCommon causes and fixes:\n\n1. **Non-deterministic values** — \`Date.now()\`, \`Math.random()\`, \`new Date().toLocaleString()\` with locale/timezone drift.\n    Fix: compute on the server and pass as a prop, or defer to \`useEffect\` (client-only).\n\n2. **Browser-only APIs during render** — reading \`window.innerWidth\`, \`localStorage\`, \`matchMedia\` inline.\n    Fix: initialize with a safe default; update in \`useEffect\`. Or use \`useSyncExternalStore\` with a proper \`getServerSnapshot\`.\n\n3. **User locale differences** — \`toLocaleDateString()\` renders differently server vs client.\n    Fix: format explicitly (e.g., \`Intl.DateTimeFormat('en-US', {...})\`) or format only on the client.\n\n4. **Invalid HTML nesting** — \`<p><div/></p>\`, \`<a><a/></a>\`, \`<button><button/></button>\`. Browsers "fix" the DOM before React hydrates.\n    Fix: fix the markup. Use the browser console — the parser will show what it actually produced.\n\n5. **Extension-injected DOM** — Grammarly, LastPass adding attributes like \`data-gramm\` to inputs.\n    Fix: add \`suppressHydrationWarning\` on the specific element (last resort, not for real bugs).\n\n6. **Conditional rendering based on \`typeof window\`** — different branches server vs client.\n    Fix: render the SAME thing on first client render, then flip in \`useEffect\`. Or use \`useSyncExternalStore\`.\n\nDebugging tip: React 18+ shows a diff between expected and received HTML in the console. Read it — it usually points straight at the offending node.`,
      keyPoints: [
        "Cause: first client render diverges from server HTML",
        "Non-deterministic values (Date.now, random) -> compute once, pass down",
        "Browser APIs in render -> defer to useEffect / useSyncExternalStore",
        "Invalid HTML nesting -> fix the markup",
        "Third-party DOM injection -> suppressHydrationWarning (surgical)",
        "typeof window branches -> use effect flip after mount",
      ],
    },
    {
      id: "mr-12",
      difficulty: "medium",
      q: "What's the difference between `useDeferredValue` and `useTransition` in one interview minute — and when do you pick which?",
      hint: "Setter vs value. Own the state -> transition. Don't own it -> deferred.",
      answer: `Both defer non-urgent work under React 18's concurrent renderer. The pick is about **who owns the state**:\n\n- **\`useTransition\`** — you own the setter. Wrap the \`setState\` call:\n\`\`\`js\nconst [isPending, startTransition] = useTransition();\nstartTransition(() => setFilter(next));\n\`\`\`\n\n- **\`useDeferredValue\`** — you got the value from a prop, context, or a controlled input you can't easily wrap. Get a "lagging copy":\n\`\`\`js\nconst deferredQuery = useDeferredValue(query);\n<HeavyList query={deferredQuery} />\n\`\`\`\n\nBoth let urgent updates (typing, clicks) preempt the deferred work. Pair the heavy consumer with \`React.memo\` so it actually skips renders when the deferred value hasn't advanced.\n\nInterview one-liner: "\`useTransition\` marks an UPDATE non-urgent; \`useDeferredValue\` marks a VALUE non-urgent. Same engine, different entry point."`,
      keyPoints: [
        "Transition wraps the setter you own",
        "DeferredValue wraps a value you got from elsewhere",
        "Both defer non-urgent work + yield to urgent",
        "Pair with React.memo on the heavy child",
        "Same underlying concurrent engine",
      ],
    },
  ],
});
