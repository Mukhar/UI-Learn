// Next.js / SSR / RSC — interview-focused: App Router, fetch caching,
// ISR, Server Actions vs Route Handlers, streaming, dynamic rendering triggers.
window.MODULES = window.MODULES || [];
window.MODULES.push({
  id: "nextjs-ssr",
  title: "Next.js / SSR / RSC",
  blurb: "App Router, fetch caching, ISR + on-demand revalidation, Server Actions, dynamic rendering, streaming — the Next.js questions that come up in senior interviews.",
  questions: [
    {
      id: "nx-1",
      difficulty: "easy",
      q: "Name the rendering strategies Next.js supports and give a one-line use case for each.",
      hint: "SSG, SSR, ISR, CSR, RSC, streaming.",
      answer: `Next.js supports a spectrum, not a binary:\n\n1. **SSG (Static Site Generation)** — HTML rendered at build time and cached forever. Use for content that rarely changes: marketing pages, docs, changelogs.\n2. **SSR (Server-Side Rendering)** — HTML rendered on every request. Use when the page depends on request-time data (user session, cookies, live data).\n3. **ISR (Incremental Static Regeneration)** — start with SSG, but regenerate the page in the background after N seconds. Best of both worlds for content that changes but not per-request: product pages, blog posts.\n4. **CSR (Client-Side Rendering)** — send a shell, render in the browser via \`'use client'\`. Use for highly interactive dashboards where SSR doesn't buy you SEO.\n5. **RSC (React Server Components)** — new in App Router. Components render on the server and their JS never ships to the client. Default for all components in \`app/\`.\n6. **Streaming SSR** — send HTML in chunks as data resolves, using \`<Suspense>\` boundaries. Fast TTFB even when parts of the page are slow.\n\nApp Router unifies all of these — the SAME page can mix RSC + streaming + client components + ISR-cached fetches. Old Pages Router was one strategy per page (via \`getStaticProps\` / \`getServerSideProps\` / \`getStaticPaths\`).`,
      keyPoints: [
        "SSG = build-time HTML",
        "SSR = per-request HTML",
        "ISR = SSG + background regeneration",
        "CSR = browser-rendered from a shell",
        "RSC = server-only, JS never ships",
        "Streaming = chunked HTML via Suspense",
        "App Router mixes all of these on one page",
      ],
    },
    {
      id: "nx-2",
      difficulty: "medium",
      q: "**App Router** vs **Pages Router** — what actually changed?",
      hint: "Layouts, RSC-by-default, colocation, new data-fetching model.",
      answer: `The App Router (in \`app/\`) is a ground-up rewrite around React Server Components. Key differences vs the classic Pages Router (\`pages/\`):\n\n1. **Server Components by default** — every file in \`app/\` is an RSC unless it starts with \`'use client'\`. In \`pages/\`, everything ran on both server and client.\n2. **Nested layouts** — \`layout.tsx\` files compose down the tree. A layout wrapping a route segment persists across navigation within that segment (no re-render of the shell). Pages Router had one global \`_app.tsx\` layout and it was awkward to nest.\n3. **Colocation** — the routing folder can contain \`page.tsx\`, \`layout.tsx\`, \`loading.tsx\`, \`error.tsx\`, \`not-found.tsx\`, plus your components and tests. Pages Router routed strictly by \`pages/*.tsx\`.\n4. **Data fetching moved to \`fetch\`** — no more \`getServerSideProps\`/\`getStaticProps\`. You just \`await fetch(url)\` inside a Server Component, and Next.js extends \`fetch\` with cache/ISR/tag semantics.\n5. **Streaming + Suspense** — first-class. Wrap a slow subtree in \`<Suspense fallback={...}>\` and it streams independently.\n6. **Route Handlers** replace API Routes — \`app/api/foo/route.ts\` exports \`GET\`/\`POST\`/etc. functions instead of a single default handler.\n7. **Server Actions** — mutations via \`'use server'\` functions callable from client forms. Pages Router had no equivalent.\n\nInterview soundbite: "Pages Router was 'React with better routing and data fetching'. App Router is 'React with a server model' — RSC changes the mental model, not just the file layout."`,
      keyPoints: [
        "App Router = RSC-by-default, Pages = client-and-server components",
        "Nested layouts persist across intra-segment nav",
        "fetch replaces getServerSideProps / getStaticProps",
        "Streaming + Suspense are first-class",
        "Route Handlers replace API Routes",
        "Server Actions for mutations (no equivalent in Pages)",
      ],
    },
    {
      id: "nx-3",
      difficulty: "hard",
      q: "Explain Next.js's extended `fetch` in App Router. What are the caching options and how do they interact?",
      hint: "Default is cache. Opt out with no-store. Opt into ISR with next.revalidate. Tag for on-demand.",
      answer: `In the App Router, Next wraps \`fetch\` with a cache layer. The behavior of every server-side \`fetch\` call is controlled by two options:\n\n\`\`\`ts\nfetch(url, {\n  cache: 'force-cache' | 'no-store' | 'no-cache' | 'reload',\n  next: {\n    revalidate: number | false,  // ISR: re-fetch after N seconds\n    tags: string[],              // for on-demand revalidation\n  },\n});\n\`\`\`\n\nThe three shapes you'll actually use:\n\n1. **Static (default in Next 14)** — \`cache: 'force-cache'\` or omitted. Fetched at build time; result cached indefinitely. This is SSG behavior.\n\`\`\`ts\nconst data = await fetch('/api/posts');  // cached forever\n\`\`\`\n\n2. **Dynamic** — \`cache: 'no-store'\`. Fetched on every request. This makes the whole route dynamic (SSR).\n\`\`\`ts\nconst me = await fetch('/api/me', { cache: 'no-store' });\n\`\`\`\n\n3. **ISR** — \`next: { revalidate: 60 }\`. Cache for 60 seconds; after that, first request triggers a background refetch, next requests get the new value.\n\`\`\`ts\nconst posts = await fetch('/api/posts', { next: { revalidate: 60 } });\n\`\`\`\n\n**On-demand revalidation** with tags:\n\`\`\`ts\n// in a Server Component / Route Handler:\nconst posts = await fetch('/api/posts', { next: { tags: ['posts'] } });\n\n// in a Server Action after a mutation:\nimport { revalidateTag } from 'next/cache';\nawait db.posts.create({ ... });\nrevalidateTag('posts');   // invalidates any fetch cached with that tag\n\`\`\`\n\nInterview traps:\n- \`cache: 'no-store'\` propagates: any route that calls it becomes dynamic. You can't have "one dynamic fetch inside an otherwise static page."\n- **Next 15 flipped the default** to \`cache: 'no-store'\`. Older tutorials assume opposite defaults. Always state your Next version.\n- \`revalidatePath('/blog')\` invalidates ALL caches for that route; \`revalidateTag('posts')\` is surgical.`,
      keyPoints: [
        "Default (Next 14): cached; Next 15: no-store default flipped",
        "cache: 'no-store' -> dynamic (SSR) rendering for the whole route",
        "next.revalidate: N -> ISR with time-based invalidation",
        "next.tags + revalidateTag() -> on-demand surgical invalidation",
        "revalidatePath() invalidates by route (broader)",
        "One no-store fetch propagates dynamism to the whole route",
      ],
    },
    {
      id: "nx-4",
      difficulty: "medium",
      q: "Design an on-demand ISR flow: static blog posts that need to update immediately when the CMS webhook fires.",
      hint: "Tag the fetch, hit a Route Handler from the webhook, revalidateTag.",
      answer: `\`\`\`ts\n// app/blog/[slug]/page.tsx  (Server Component)\nexport default async function BlogPost({ params }: { params: { slug: string } }) {\n  const post = await fetch(\`\${CMS}/posts/\${params.slug}\`, {\n    next: { tags: [\`post:\${params.slug}\`, 'posts'] },\n  }).then(r => r.json());\n\n  return <article><h1>{post.title}</h1>{/* ... */}</article>;\n}\n\n// app/api/revalidate/route.ts  (Route Handler the CMS webhook posts to)\nimport { revalidateTag } from 'next/cache';\nimport { NextRequest, NextResponse } from 'next/server';\n\nexport async function POST(req: NextRequest) {\n  const secret = req.headers.get('x-webhook-secret');\n  if (secret !== process.env.CMS_WEBHOOK_SECRET) {\n    return NextResponse.json({ ok: false }, { status: 401 });\n  }\n  const { slug } = await req.json();\n  revalidateTag(\`post:\${slug}\`);   // surgical: only that post\n  return NextResponse.json({ ok: true, revalidated: slug });\n}\n\`\`\`\n\nWhy this flow is the right one:\n1. **Static by default** — every post is pre-rendered and served from the cache. Fast, cheap.\n2. **Two tags per post** — a specific one (\`post:my-slug\`) and a category tag (\`posts\`). Lets you invalidate one post OR all posts.\n3. **Webhook auth** — never expose \`/api/revalidate\` without a secret. Anyone hitting it can force refetches (mini-DoS + traffic amplification).\n4. **Instant propagation** — no \`revalidate: 60\` polling window. The next request after \`revalidateTag\` fires a fresh fetch and re-caches.\n\nAlternative for scheduled refreshes without a webhook: keep \`next.tags\` for the manual case AND add \`next: { revalidate: 3600 }\` as a safety net so stale content never lasts more than an hour.`,
      keyPoints: [
        "Tag fetches with granular AND coarse tags",
        "Route Handler receives the webhook and calls revalidateTag",
        "Always authenticate revalidation endpoints (secret header)",
        "Combine tags with a revalidate: N safety net for the manual-miss case",
        "revalidateTag is O(1) surgical vs revalidatePath = whole route",
      ],
    },
    {
      id: "nx-5",
      difficulty: "hard",
      q: "**Route Handlers** vs **Server Actions** — when do you pick which?",
      hint: "Actions for internal form/mutation flows; Route Handlers for public HTTP endpoints.",
      answer: `Both run on the server, both can mutate data. The distinction is about the **consumer**:\n\n**Server Actions** (\`'use server'\` functions):\n- Called from your own React components (usually a \`<form action={fn}>\` or via a Client Component invocation).\n- Not a real HTTP endpoint you'd call from \`curl\` — Next tunnels the invocation over its own RSC transport, encodes args, handles CSRF-ish behavior.\n- Return values are automatically wired into \`useActionState\` state.\n- Best for: form submissions, likes, follows, "click this button to do X" flows.\n\n\`\`\`ts\n// app/actions.ts\n'use server';\nexport async function createTodo(formData: FormData) {\n  await db.todos.insert({ text: formData.get('text') as string });\n  revalidateTag('todos');\n}\n\n// app/todos/page.tsx\nimport { createTodo } from '../actions';\nexport default function Page() {\n  return <form action={createTodo}><input name=\"text\" /><button /></form>;\n}\n\`\`\`\n\n**Route Handlers** (\`app/api/*/route.ts\`):\n- Real HTTP endpoints — you (or third parties) can \`fetch\`/\`curl\`/webhook them.\n- Full control over method, headers, status, streaming responses.\n- Best for: webhooks, public APIs, endpoints called by non-React clients (mobile app, cron), file uploads/downloads, OAuth callbacks, revalidation triggers.\n\n\`\`\`ts\n// app/api/webhooks/stripe/route.ts\nexport async function POST(req: Request) {\n  const sig = req.headers.get('stripe-signature');\n  const event = stripe.webhooks.constructEvent(await req.text(), sig!, SECRET);\n  // ... handle event ...\n  return Response.json({ received: true });\n}\n\`\`\`\n\nDecision matrix:\n- **My own UI calls it** -> Server Action.\n- **Anything else calls it** -> Route Handler.\n- **Public REST/GraphQL API** -> Route Handler.\n- **Form mutation from a React component** -> Server Action.\n- **Webhook / cron / mobile client** -> Route Handler.\n\nCommon mistake: building a Route Handler \`POST /api/create-todo\` and calling \`fetch\` from the React form. Wastes network hops, loses the Actions integration (pending state, optimistic UI, form reset, progressive enhancement). Use an Action.`,
      keyPoints: [
        "Server Action = called by your own React tree via RSC transport",
        "Route Handler = real HTTP endpoint for anyone",
        "Actions integrate with useActionState / useFormStatus / useOptimistic",
        "Route Handlers for webhooks, public APIs, non-React callers",
        "Don't build a Route Handler just to POST from your own form — use an Action",
      ],
    },
    {
      id: "nx-6",
      difficulty: "medium",
      q: "`generateStaticParams` — what does it do, and what happens when a user requests a slug you didn't pre-generate?",
      hint: "Pre-render dynamic routes at build time. `dynamicParams` controls the fallback.",
      answer: `\`generateStaticParams\` tells Next which values of a dynamic route segment to pre-render at build time. It replaces Pages Router's \`getStaticPaths\`.\n\n\`\`\`ts\n// app/blog/[slug]/page.tsx\nexport async function generateStaticParams() {\n  const posts = await fetch(\`\${CMS}/posts\`).then(r => r.json());\n  return posts.map((p: { slug: string }) => ({ slug: p.slug }));\n  // Next pre-renders /blog/hello, /blog/goodbye, etc.\n}\n\nexport default async function Post({ params }: { params: { slug: string } }) {\n  const post = await fetch(\`\${CMS}/posts/\${params.slug}\`).then(r => r.json());\n  return <article>{/* ... */}</article>;\n}\n\`\`\`\n\nWhat happens for a slug NOT in the returned list is controlled by \`dynamicParams\` (default: \`true\`):\n\n- **\`dynamicParams = true\`** (default): unknown slugs are rendered on-demand at request time (like ISR) and then cached. New CMS posts "just work" without a rebuild.\n- **\`dynamicParams = false\`**: unknown slugs return a 404. Useful when your set of valid slugs is closed (e.g., a fixed enum of categories).\n\n\`\`\`ts\nexport const dynamicParams = false; // 404 for anything not in generateStaticParams\n\`\`\`\n\nWhy you'd use each:\n- \`true\` (default) — user-generated content, CMS-backed pages where new items shouldn't require a rebuild.\n- \`false\` — a fixed list of things where a typo in the URL should 404 instead of hitting your DB with garbage.\n\nBonus: \`generateStaticParams\` for **multiple dynamic segments** returns objects with all keys:\n\`\`\`ts\n// app/[category]/[slug]/page.tsx\nexport async function generateStaticParams() {\n  return [\n    { category: 'tech', slug: 'react-18' },\n    { category: 'life', slug: 'coffee' },\n  ];\n}\n\`\`\``,
      keyPoints: [
        "Replaces getStaticPaths — returns list of param combos to pre-render",
        "dynamicParams: true (default) renders unknown slugs on-demand + caches",
        "dynamicParams: false 404s anything not in the list",
        "Combined with fetch caching for a full SSG/ISR pipeline",
        "Multi-segment routes return objects with all keys",
      ],
    },
    {
      id: "nx-7",
      difficulty: "medium",
      q: "What does `next/image` actually do that a plain `<img>` doesn't? Sketch the API.",
      hint: "Automatic responsive srcset, format negotiation, lazy loading, CLS prevention.",
      answer: `\`next/image\` is a wrapper around \`<img>\` that handles a bunch of image performance concerns automatically:\n\n1. **Responsive \`srcset\` generation** — serves the right-sized image for each viewport. No hand-writing \`srcset="… 320w, … 640w, …"\`.\n2. **Format negotiation** — serves AVIF/WebP to browsers that support it, falls back to JPEG/PNG. Massive size reduction for free.\n3. **On-the-fly resizing** — the built-in Image Optimization API (or a plugged-in loader like Cloudinary/imgix) generates sizes on demand and caches them.\n4. **Lazy loading by default** — \`loading="lazy"\` unless you set \`priority\` (which also adds \`fetchpriority="high"\` and preload hints).\n5. **CLS prevention** — requires \`width\`/\`height\` OR the \`fill\` prop, so the browser reserves space before the image loads. No layout shift.\n6. **Blur placeholder** — \`placeholder="blur"\` with either a static import (auto blur data URL) or a supplied \`blurDataURL\`.\n\n\`\`\`tsx\nimport Image from 'next/image';\nimport hero from './hero.jpg'; // static import: width/height/blur inferred\n\n// Fixed size\n<Image src={hero} alt=\"Hero shot\" priority placeholder=\"blur\" />\n\n// Remote image (must declare hostname in next.config.js)\n<Image src=\"https://cdn.example.com/x.jpg\" alt=\"...\" width={800} height={600} />\n\n// Fill parent (container needs position: relative)\n<div className=\"relative w-full h-64\">\n  <Image src=\"/bg.jpg\" alt=\"...\" fill className=\"object-cover\" />\n</div>\n\`\`\`\n\nGotchas interviewers love:\n- Remote images need \`images.remotePatterns\` (or the older \`domains\`) in \`next.config.js\` — otherwise it 404s.\n- \`priority\` should be on your LCP image only. Don't spray it on everything.\n- \`fill\` requires a positioned parent; forgetting it means the image collapses.\n- \`alt\` is required in TS types — accessibility win.\n\nWhen to skip it: purely decorative CSS backgrounds, or when you already have a CDN with equivalent features and don't want the Next optimizer in the loop.`,
      keyPoints: [
        "Responsive srcset + format negotiation (AVIF/WebP) automatically",
        "Lazy-load by default; priority for LCP image only",
        "Requires width/height or fill -> prevents CLS",
        "Blur placeholder via static import or blurDataURL",
        "Remote hosts must be allowlisted in next.config.js",
      ],
    },
    {
      id: "nx-8",
      difficulty: "medium",
      q: "What does `<Link>` do that a plain `<a>` doesn't, and how does prefetching behave?",
      hint: "Client-side navigation + prefetch on viewport intersection (in production).",
      answer: `\`next/link\` is a wrapper around \`<a>\` that does two things:\n\n1. **Client-side navigation** — intercepts the click, uses the App Router to swap the page without a full document reload. Preserves scroll position, keeps client state, doesn't re-run RSCs above the shared layout.\n2. **Prefetching** — in production, when a \`<Link>\` enters the viewport, Next fetches the target route's RSC payload (and JS chunks) in the background. Clicking then navigates instantly because everything's already downloaded.\n\n\`\`\`tsx\nimport Link from 'next/link';\n\n<Link href=\"/blog\">Blog</Link>                       // default prefetch\n<Link href=\"/heavy\" prefetch={false}>Heavy page</Link> // opt out for rarely-clicked links\n<Link href=\"/dashboard\" prefetch scroll={false}>...</Link> // don't scroll to top on nav\n<Link href=\"/x\" replace>Replace history entry</Link>\n\`\`\`\n\nWhat prefetch actually fetches:\n- **Static routes**: the full RSC payload + JS chunks.\n- **Dynamic routes**: only the shared layouts (with a loading UI) — the dynamic \`page.tsx\` is fetched on click.\n- **In dev**: prefetching is DISABLED to avoid confusing you with cache-hits during iteration.\n\nWhen to disable prefetching (\`prefetch={false}\`):\n- Rarely-clicked links (footer legal pages) — prefetch cost > benefit.\n- Data-heavy pages you don't want speculatively fetched (analytics, expensive DB queries).\n- Pagination "next page" links when there are hundreds of them in a table.\n\nInterview trap: prefetching only fires when the link is in the viewport (via IntersectionObserver). A sidebar link in a scroll container might not prefetch until scrolled into view — that's intentional, not a bug.`,
      keyPoints: [
        "Client-side nav: no full reload, keeps state, preserves layout tree",
        "Prefetch fires on viewport intersection in production",
        "Static routes prefetch full payload; dynamic routes only prefetch layouts",
        "Prefetching disabled in dev",
        "Opt out with prefetch={false} for rarely-clicked or heavy links",
      ],
    },
    {
      id: "nx-9",
      difficulty: "medium",
      q: "Walk me through the App Router file conventions: `page`, `layout`, `loading`, `error`, `not-found`, `template`. What does each do?",
      hint: "Composed automatically by folder structure.",
      answer: `A route segment folder can contain these special files, which Next auto-composes:\n\n**\`page.tsx\`** — the actual page content for the route. Required to make a segment publicly accessible.\n\n**\`layout.tsx\`** — a Server Component that wraps its \`page\` AND all nested pages. Persists across intra-segment navigation — its state, its DOM, its position — no re-render. Must render \`{children}\`.\n\n\`\`\`tsx\n// app/dashboard/layout.tsx — wraps every /dashboard/* page\nexport default function DashLayout({ children }: { children: ReactNode }) {\n  return <div><Sidebar />{children}</div>;\n}\n\`\`\`\n\n**\`loading.tsx\`** — automatic Suspense fallback for the segment. When you navigate to a slow page, this shows instantly while the RSC data resolves.\n\n\`\`\`tsx\nexport default function Loading() { return <Spinner />; }\n// Equivalent to wrapping <page /> in <Suspense fallback={<Spinner />}> upstream.\n\`\`\`\n\n**\`error.tsx\`** — a Client Component (must have \`'use client'\`) that acts as an Error Boundary for the segment. Receives \`{ error, reset }\` props. Catches errors thrown in the segment (both server and client) without crashing the whole app.\n\n\`\`\`tsx\n'use client';\nexport default function Err({ error, reset }: { error: Error; reset(): void }) {\n  return <><p>{error.message}</p><button onClick={reset}>Try again</button></>;\n}\n\`\`\`\n\n**\`not-found.tsx\`** — the UI shown when \`notFound()\` is called or the route/param doesn't match. Segment-scoped 404.\n\n**\`template.tsx\`** — like \`layout\` BUT re-renders on every navigation. State inside a template is discarded per-route. Rare use: page transition animations that must remount.\n\n**\`route.ts\`** — mutually exclusive with \`page.tsx\`. Turns the folder into an HTTP endpoint (Route Handler) instead of a page.\n\nComposition order (outer to inner):\n\n\`\`\`\n<Layout>\n  <Template>\n    <ErrorBoundary fallback={<Error/>}>\n      <Suspense fallback={<Loading/>}>\n        <NotFoundBoundary fallback={<NotFound/>}>\n          <Page />\n        </NotFoundBoundary>\n      </Suspense>\n    </ErrorBoundary>\n  </Template>\n</Layout>\n\`\`\`\n\nThis auto-wrapping is what makes App Router feel "batteries included" — you don't wire Suspense/ErrorBoundary/Layout by hand for every route.`,
      keyPoints: [
        "page.tsx = the route's content",
        "layout.tsx = persistent wrapper, no re-render across intra-segment nav",
        "loading.tsx = automatic Suspense fallback",
        "error.tsx = client-component Error Boundary with error + reset",
        "not-found.tsx = segment-scoped 404",
        "template.tsx = layout that DOES remount (rare)",
        "route.ts = HTTP endpoint (mutually exclusive with page)",
      ],
    },
    {
      id: "nx-10",
      difficulty: "hard",
      q: "What are the **dynamic rendering triggers** in App Router? Why does calling `cookies()` or `headers()` make a route fully dynamic?",
      hint: "Any request-only API forces per-request rendering.",
      answer: `Next.js decides at build time whether each route can be pre-rendered (static) or must be rendered per-request (dynamic). Certain APIs are **dynamic-only** — using them anywhere in the route tree forces the whole route into dynamic rendering.\n\nThe triggers:\n\n1. **\`cookies()\` / \`headers()\`** — reading the incoming request's cookies or headers. Only available at request time.\n\`\`\`ts\nimport { cookies, headers } from 'next/headers';\nconst session = cookies().get('session')?.value; // -> whole route becomes dynamic\n\`\`\`\n\n2. **\`searchParams\` prop on \`page.tsx\`** — since query strings vary per request.\n\`\`\`tsx\nexport default function Page({ searchParams }: { searchParams: { q?: string } }) {\n  // reading searchParams -> dynamic\n}\n\`\`\`\n\n3. **\`fetch(url, { cache: 'no-store' })\`** or **\`{ next: { revalidate: 0 } }\`** — asking for uncached data.\n\n4. **\`unstable_noStore()\`** (or \`noStore()\` in newer Next) — explicit opt-in to dynamic rendering without needing a fetch.\n\n5. **Route segment config**: \`export const dynamic = 'force-dynamic';\`\n\nWhy this cascades: RSCs can't be pre-rendered without knowing the request. If ANY component in the tree reads request-specific data, the whole render has to happen per-request. Next's compiler tracks this and marks the route dynamic.\n\nThe inverse: to keep a route static, avoid all triggers. Use \`getServerSession\`-like helpers only in dynamic routes; keep the marketing pages free of \`cookies()\` calls.\n\nSubtle gotcha: even \`console.log(headers())\` in a helper called from a Server Component makes the route dynamic. The static analyzer isn't checking whether you USED the value — just whether you called the API.\n\n**Partial Prerendering (PPR)** — a newer Next feature — lets you have a static shell with dynamic "holes" (Suspense boundaries that render per-request). This resolves the "one dynamic call ruins the whole route" pain, but is still stabilizing.`,
      keyPoints: [
        "cookies(), headers(), searchParams -> force dynamic",
        "fetch({ cache: 'no-store' }) or { revalidate: 0 } -> dynamic",
        "export const dynamic = 'force-dynamic' -> explicit opt-in",
        "One dynamic API anywhere in the tree cascades to the whole route",
        "Analyzer checks that you CALLED the API, not that you used the result",
        "Partial Prerendering (PPR) is the escape hatch for static shell + dynamic islands",
      ],
    },
    {
      id: "nx-11",
      difficulty: "hard",
      q: "When do you reach for `next/dynamic` with `{ ssr: false }`, and what's the tradeoff?",
      hint: "Client-only components (map libraries, editors). Cost: SEO + hydration flicker.",
      answer: `\`next/dynamic\` is Next's wrapper around \`React.lazy\` + \`Suspense\` with extra options — most importantly \`ssr: false\`, which skips server rendering entirely for a component.\n\n\`\`\`tsx\nimport dynamic from 'next/dynamic';\n\nconst Map = dynamic(() => import('./Map'), {\n  ssr: false,\n  loading: () => <div className=\"h-64 bg-slate-100 animate-pulse\" />,\n});\n\nexport default function Page() {\n  return <><h1>Store locator</h1><Map /></>;\n}\n\`\`\`\n\nWhen you need it:\n\n1. **Libraries that touch \`window\` / \`document\` on import** — Leaflet, some chart libs, WYSIWYG editors (TipTap, Quill), video players. Server-rendering them throws \`window is not defined\`.\n2. **Heavy client-only widgets** — a full-featured code editor (Monaco) that nobody sees until they interact. Skip the SSR cost.\n3. **Third-party embeds that fingerprint the browser** — analytics, session replay, chat widgets. SSR wastes cycles.\n\nThe tradeoffs:\n\n- **SEO** — anything inside is invisible to crawlers relying on server HTML. Fine for interactive tools, bad for content.\n- **Hydration flicker** — the space is empty (or shows your \`loading\`) until JS loads and the component mounts. Reserve height in the loading fallback to prevent layout shift.\n- **LCP** — if the dynamic component IS the largest painted element, your Core Web Vitals will suffer.\n\nWhat NOT to do:\n- Don't use \`dynamic({ ssr: false })\` just to fix a hydration mismatch bug. That's papering over the real issue (non-deterministic render). Fix the source.\n- Don't wrap every "big" component. Only reach for it when a real SSR incompatibility or a real perf win justifies it.\n\nApp Router notes: in App Router you can achieve the same with a Client Component (\`'use client'\`) using \`React.lazy\` + \`Suspense\` directly, but \`next/dynamic\` still works and has the nice \`loading\` shortcut. The \`ssr: false\` flag is only usable from Client Components in App Router.`,
      keyPoints: [
        "ssr: false = skip server render, load client-only",
        "Use case: libs that need window/document, huge client-only widgets",
        "Cost: not in SEO HTML, hydration flicker, potential LCP hit",
        "Reserve space in the loading fallback to prevent CLS",
        "Don't use it to hide hydration mismatches — fix the real cause",
        "App Router: ssr:false only valid inside Client Components",
      ],
    },
    {
      id: "nx-12",
      difficulty: "hard",
      q: "Explain **middleware** in Next.js: what can it do, where does it run, and what are the Edge runtime limits?",
      hint: "Runs before every matched request, at the edge, with a limited API surface.",
      answer: `Next middleware is a single file at the project root (\`middleware.ts\`) that runs BEFORE every matching request — before routing, before Route Handlers, before pages. It runs on the **Edge runtime** by default (not Node.js), which is a lightweight V8-based sandbox deployed close to users.\n\n\`\`\`ts\n// middleware.ts\nimport { NextResponse, type NextRequest } from 'next/server';\n\nexport function middleware(req: NextRequest) {\n  const session = req.cookies.get('session')?.value;\n\n  // 1. Redirect\n  if (!session && req.nextUrl.pathname.startsWith('/dashboard')) {\n    return NextResponse.redirect(new URL('/login', req.url));\n  }\n\n  // 2. Rewrite (URL stays but different page renders)\n  if (req.nextUrl.pathname === '/old') {\n    return NextResponse.rewrite(new URL('/new', req.url));\n  }\n\n  // 3. Set headers / cookies on the response\n  const res = NextResponse.next();\n  res.headers.set('x-country', req.geo?.country ?? 'US');\n  return res;\n}\n\nexport const config = {\n  matcher: ['/dashboard/:path*', '/old', '/((?!api|_next/static|_next/image|favicon.ico).*)'],\n};\n\`\`\`\n\nWhat middleware is good for:\n- **Auth gating** — redirect unauthenticated users away from protected areas.\n- **A/B tests** — rewrite to different variant pages based on a cookie.\n- **Geo/locale routing** — redirect \`/en\` visitors from Germany to \`/de\`.\n- **Bot detection / rate limiting** — inspect UA + IP, return 429.\n- **Setting request/response headers** — CSP, security headers, request IDs.\n\nEdge runtime limits (why you can't just move Node code here):\n1. **No Node APIs** — no \`fs\`, no \`child_process\`, no native Node modules. Not even \`Buffer\` in some versions.\n2. **No dynamic \`require()\`** — must be static ESM imports (statically analyzable).\n3. **Size cap** — the bundled middleware code is capped (~1-4 MB depending on host). Big deps like Prisma, jsonwebtoken (with crypto polyfills), heavy validators can bust it.\n4. **Limited execution time** — typically ~30s max, but you want <10ms since it runs on every request.\n5. **No streaming responses** — return once.\n6. **Web APIs only** — \`fetch\`, \`URL\`, \`Request\`, \`Response\`, \`crypto.subtle\`, \`TextEncoder\`. Node-specific things are absent.\n\nEscape hatch: \`export const runtime = 'nodejs'\` on individual Route Handlers (NOT middleware) if you need the full Node API. Middleware is Edge-only.\n\nInterview traps:\n- Middleware runs on EVERY matched request — do not do heavy work here (DB queries, big JWT verifications). Use it for cheap decisions; delegate the rest.\n- \`matcher\` must be a static string/array — no dynamic patterns.\n- Middleware CANNOT read request body (unless you consume it explicitly, and even then be careful — you'll block downstream handlers from reading it).`,
      keyPoints: [
        "Runs BEFORE routing, on Edge runtime",
        "Capabilities: redirect, rewrite, set headers/cookies, next()",
        "Use cases: auth, A/B, geo routing, security headers, rate limit",
        "Edge limits: no Node APIs, size cap, must use Web APIs only",
        "Runs on every match — keep it fast, delegate heavy work",
        "matcher config must be static; escape via runtime='nodejs' only on Route Handlers",
      ],
    },
  ],
});
